include key
include info

#elements
  //- include special
  for element in locals.filter(function(el){return !(el.get("Category") == "lanthanide" || el.get("Category") == "actinide");})
    
    include element

  hr.separator

  for element, i in locals.filter(function(el){return (el.get("Category") == "lanthanide" || el.get("Category") == "actinide");})
    include element
